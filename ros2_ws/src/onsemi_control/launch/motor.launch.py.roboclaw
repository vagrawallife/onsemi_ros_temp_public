import os

from ament_index_python.packages import get_package_share_directory


from launch import LaunchDescription
from launch.actions import IncludeLaunchDescription, TimerAction
from launch.launch_description_sources import PythonLaunchDescriptionSource
from launch.actions import RegisterEventHandler
from launch.event_handlers import OnProcessStart
from launch.substitutions import Command, FindExecutable, PathJoinSubstitution, LaunchConfiguration
from launch_ros.actions import Node
from launch_ros.substitutions import FindPackageShare

package_name='onsemi_control'

def generate_launch_description():

    motor_info = PathJoinSubstitution([ FindPackageShare("onsemi_control"), "config", "motor_driver.yaml",  ])

    ld = LaunchDescription()


    motor_drive_node= Node(
        name='onsemi_motor_node',
        package='onsemi_control',
        executable='onsemi_motor_node',
        parameters=[motor_info],
        output='screen',
    ),
    
    ld.add_action(motor_drive_node)
    return ld